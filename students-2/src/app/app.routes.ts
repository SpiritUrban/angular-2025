import { Routes } from '@angular/router';
import { SubjectComponent } from './pages/ngrx/subject/subject.component';

export const routes: Routes = [
  { path: 'subject', component: SubjectComponent }
];
